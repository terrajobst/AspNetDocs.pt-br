---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: Introdução ao Páginas da Web do ASP.NET-noções básicas de formulário HTML | Microsoft Docs
author: Rick-Anderson
description: Este tutorial mostra as noções básicas de como criar um formulário de entrada e como tratar a entrada do usuário quando você usa Páginas da Web do ASP.NET (Razor). E agora que você...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: f57661077ec3bb13f3d4ec41b130bda4d2fb9070
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 03/06/2020
ms.locfileid: "78574280"
---
# <a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="2012e-104">Introdução ao Páginas da Web do ASP.NET-noções básicas de formulários HTML</span><span class="sxs-lookup"><span data-stu-id="2012e-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>

<span data-ttu-id="2012e-105">por [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="2012e-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="2012e-106">Este tutorial mostra as noções básicas de como criar um formulário de entrada e como tratar a entrada do usuário quando você usa Páginas da Web do ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="2012e-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="2012e-107">E agora que você tem um banco de dados, você usará suas habilidades de formulário para permitir que os usuários localizem filmes específicos no banco de dados.</span><span class="sxs-lookup"><span data-stu-id="2012e-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="2012e-108">Ele pressupõe que você concluiu a série por meio da [introdução à exibição de dados usando páginas da Web do ASP.net](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span><span class="sxs-lookup"><span data-stu-id="2012e-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="2012e-109">O que você aprenderá:</span><span class="sxs-lookup"><span data-stu-id="2012e-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="2012e-110">Como criar um formulário usando elementos HTML padrão.</span><span class="sxs-lookup"><span data-stu-id="2012e-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="2012e-111">Como ler a entrada do usuário em um formulário.</span><span class="sxs-lookup"><span data-stu-id="2012e-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="2012e-112">Como criar uma consulta SQL que obtém dados seletivamente usando um termo de pesquisa que o usuário fornece.</span><span class="sxs-lookup"><span data-stu-id="2012e-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="2012e-113">Como os campos na página "lembram" o que o usuário inseriu.</span><span class="sxs-lookup"><span data-stu-id="2012e-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="2012e-114">Recursos/tecnologias abordados:</span><span class="sxs-lookup"><span data-stu-id="2012e-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="2012e-115">O objeto `Request`.</span><span class="sxs-lookup"><span data-stu-id="2012e-115">The `Request` object.</span></span>
> - <span data-ttu-id="2012e-116">A cláusula SQL `Where`.</span><span class="sxs-lookup"><span data-stu-id="2012e-116">The SQL `Where` clause.</span></span>

## <a name="what-youll-build"></a><span data-ttu-id="2012e-117">O que você vai construir</span><span class="sxs-lookup"><span data-stu-id="2012e-117">What You'll Build</span></span>

<span data-ttu-id="2012e-118">No tutorial anterior, você criou um banco de dados, adicionou um dado a ele e, em seguida, usou o auxiliar de `WebGrid` para exibir os dados.</span><span class="sxs-lookup"><span data-stu-id="2012e-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="2012e-119">Neste tutorial, você adicionará uma caixa de pesquisa que permite encontrar filmes de um gênero específico ou cujo título contém qualquer palavra que você inserir.</span><span class="sxs-lookup"><span data-stu-id="2012e-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="2012e-120">(Por exemplo, você poderá localizar todos os filmes cujo gênero seja "ação" ou cujo título contenha "Jaime" ou "aventura".)</span><span class="sxs-lookup"><span data-stu-id="2012e-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="2012e-121">Quando você terminar este tutorial, terá uma página como esta:</span><span class="sxs-lookup"><span data-stu-id="2012e-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![Página de filmes com a pesquisa de gênero e de título](form-basics/_static/image1.png)

<span data-ttu-id="2012e-123">A parte de listagem da página é a mesma do último tutorial &mdash; uma grade.</span><span class="sxs-lookup"><span data-stu-id="2012e-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="2012e-124">A diferença será que a grade mostrará apenas os filmes que você pesquisou.</span><span class="sxs-lookup"><span data-stu-id="2012e-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="2012e-125">Sobre formulários HTML</span><span class="sxs-lookup"><span data-stu-id="2012e-125">About HTML Forms</span></span>

<span data-ttu-id="2012e-126">(Se você tiver experiência com a criação de formulários HTML e com a diferença entre `GET` e `POST`, poderá ignorar esta seção.)</span><span class="sxs-lookup"><span data-stu-id="2012e-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="2012e-127">Um formulário tem elementos de entrada do usuário &mdash; caixas de texto, botões, botões de opção, caixas de seleção, listas suspensas e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="2012e-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="2012e-128">Os usuários preenchem esses controles ou fazem seleções e, em seguida, enviam o formulário clicando em um botão.</span><span class="sxs-lookup"><span data-stu-id="2012e-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="2012e-129">A sintaxe HTML básica de um formulário é ilustrada por este exemplo:</span><span class="sxs-lookup"><span data-stu-id="2012e-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="2012e-130">Quando essa marcação é executada em uma página, ela cria um formulário simples semelhante a esta ilustração:</span><span class="sxs-lookup"><span data-stu-id="2012e-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![Formulário HTML básico como processado no navegador](form-basics/_static/image2.png)

<span data-ttu-id="2012e-132">O elemento `<form>` inclui elementos HTML a serem enviados.</span><span class="sxs-lookup"><span data-stu-id="2012e-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="2012e-133">(Um equívoco fácil de fazer é adicionar elementos à página, mas, em seguida, esquecer de colocá-los dentro de um elemento de `<form>`.</span><span class="sxs-lookup"><span data-stu-id="2012e-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="2012e-134">Nesse caso, nada é enviado.) O atributo `method` informa ao navegador como enviar a entrada do usuário.</span><span class="sxs-lookup"><span data-stu-id="2012e-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="2012e-135">Defina isso para `post` se estiver executando uma atualização no servidor ou para `get` se você estiver apenas buscando dados do servidor.</span><span class="sxs-lookup"><span data-stu-id="2012e-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> <span data-ttu-id="2012e-136">**Segurança de verbo GET, POST e HTTP**</span><span class="sxs-lookup"><span data-stu-id="2012e-136">**GET, POST, and HTTP Verb Safety**</span></span>
> 
> <span data-ttu-id="2012e-137">O HTTP, o protocolo que os navegadores e os servidores usam para trocar informações, é notavelmente simples em suas operações básicas.</span><span class="sxs-lookup"><span data-stu-id="2012e-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="2012e-138">Os navegadores usam apenas alguns verbos para fazer solicitações aos servidores.</span><span class="sxs-lookup"><span data-stu-id="2012e-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="2012e-139">Quando você escreve o código para a Web, é útil entender esses verbos e como o navegador e o servidor os usam.</span><span class="sxs-lookup"><span data-stu-id="2012e-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="2012e-140">Os verbos mais comumente usados são:</span><span class="sxs-lookup"><span data-stu-id="2012e-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - <span data-ttu-id="2012e-141">`GET`.</span><span class="sxs-lookup"><span data-stu-id="2012e-141">`GET`.</span></span> <span data-ttu-id="2012e-142">O navegador usa esse verbo para buscar algo do servidor.</span><span class="sxs-lookup"><span data-stu-id="2012e-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="2012e-143">Por exemplo, quando você digita uma URL em seu navegador, o navegador executa uma operação de `GET` para solicitar a página desejada.</span><span class="sxs-lookup"><span data-stu-id="2012e-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="2012e-144">Se a página incluir elementos gráficos, o navegador executará operações `GET` adicionais para obter as imagens.</span><span class="sxs-lookup"><span data-stu-id="2012e-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="2012e-145">Se a operação de `GET` tiver que passar informações para o servidor, as informações serão passadas como parte da URL na cadeia de caracteres de consulta.</span><span class="sxs-lookup"><span data-stu-id="2012e-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - <span data-ttu-id="2012e-146">`POST`.</span><span class="sxs-lookup"><span data-stu-id="2012e-146">`POST`.</span></span> <span data-ttu-id="2012e-147">O navegador envia uma solicitação de `POST` para enviar dados a serem adicionados ou alterados no servidor.</span><span class="sxs-lookup"><span data-stu-id="2012e-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="2012e-148">Por exemplo, o verbo `POST` é usado para criar registros em um banco de dados ou alterar os existentes.</span><span class="sxs-lookup"><span data-stu-id="2012e-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="2012e-149">Na maioria das vezes, quando você preenche um formulário e clica no botão enviar, o navegador executa uma operação de `POST`.</span><span class="sxs-lookup"><span data-stu-id="2012e-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="2012e-150">Em uma operação `POST`, os dados que estão sendo passados para o servidor estão no corpo da página.</span><span class="sxs-lookup"><span data-stu-id="2012e-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="2012e-151">Uma distinção importante entre esses verbos é que uma operação de `GET` não deve alterar nada no servidor — ou colocá-lo de forma ligeiramente mais abstrata, uma operação de `GET` não resulta em uma alteração no estado no servidor.</span><span class="sxs-lookup"><span data-stu-id="2012e-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="2012e-152">Você pode executar uma operação `GET` nos mesmos recursos quantas vezes desejar, e esses recursos não são alterados.</span><span class="sxs-lookup"><span data-stu-id="2012e-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="2012e-153">(Geralmente, uma operação de `GET` é considerada "segura" ou usar um termo técnico, é *idempotente*.) Por outro lado, é claro que uma solicitação de `POST` altera algo no servidor cada vez que você executa a operação.</span><span class="sxs-lookup"><span data-stu-id="2012e-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="2012e-154">Dois exemplos ajudarão a ilustrar essa distinção.</span><span class="sxs-lookup"><span data-stu-id="2012e-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="2012e-155">Ao executar uma pesquisa usando um mecanismo como o Bing ou o Google, você preenche um formulário que consiste em uma caixa de texto e clica no botão Pesquisar.</span><span class="sxs-lookup"><span data-stu-id="2012e-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="2012e-156">O navegador executa uma operação `GET`, com o valor inserido na caixa passada como parte da URL.</span><span class="sxs-lookup"><span data-stu-id="2012e-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="2012e-157">O uso de uma operação de `GET` para esse tipo de formulário é bom, porque uma operação de pesquisa não altera nenhum recurso no servidor, ele apenas busca informações.</span><span class="sxs-lookup"><span data-stu-id="2012e-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="2012e-158">Agora, considere o processo de ordenar algo online.</span><span class="sxs-lookup"><span data-stu-id="2012e-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="2012e-159">Preencha os detalhes do pedido e, em seguida, clique no botão enviar.</span><span class="sxs-lookup"><span data-stu-id="2012e-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="2012e-160">Essa operação será uma `POST` solicitação, porque a operação resultará em alterações no servidor, como um novo registro de pedido, uma alteração nas informações da conta e, talvez, muitas outras alterações.</span><span class="sxs-lookup"><span data-stu-id="2012e-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="2012e-161">Ao contrário da operação de `GET`, não é possível repetir a solicitação de `POST` — se você fez isso, sempre que reenviou a solicitação, você geraria um novo pedido no servidor.</span><span class="sxs-lookup"><span data-stu-id="2012e-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="2012e-162">(Em casos como esse, os sites geralmente avisam que você não deve clicar em um botão de envio mais de uma vez ou desabilitará o botão enviar para que você não reenvie o formulário acidentalmente.)</span><span class="sxs-lookup"><span data-stu-id="2012e-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="2012e-163">No decorrer deste tutorial, você usará uma operação de `GET` e uma `POST` operação para trabalhar com formulários HTML.</span><span class="sxs-lookup"><span data-stu-id="2012e-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="2012e-164">Explicaremos em cada caso por que o verbo usado é o apropriado.</span><span class="sxs-lookup"><span data-stu-id="2012e-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="2012e-165">(Para saber mais sobre verbos HTTP, consulte o artigo [definições de método](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) no site do W3C.)</span><span class="sxs-lookup"><span data-stu-id="2012e-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>

<span data-ttu-id="2012e-166">A maioria dos elementos de entrada do usuário são elementos HTML `<input>`.</span><span class="sxs-lookup"><span data-stu-id="2012e-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="2012e-167">Eles parecem `<input type="type" name="name">,` em que *Type* indica o tipo de controle de entrada do usuário desejado.</span><span class="sxs-lookup"><span data-stu-id="2012e-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="2012e-168">Esses elementos são os comuns:</span><span class="sxs-lookup"><span data-stu-id="2012e-168">These elements are the common ones:</span></span>

- <span data-ttu-id="2012e-169">Caixa de texto: `<input type="text">`</span><span class="sxs-lookup"><span data-stu-id="2012e-169">Text box: `<input type="text">`</span></span>
- <span data-ttu-id="2012e-170">Caixa de seleção: `<input type="check">`</span><span class="sxs-lookup"><span data-stu-id="2012e-170">Check box: `<input type="check">`</span></span>
- <span data-ttu-id="2012e-171">Botão de opção: `<input type="radio">`</span><span class="sxs-lookup"><span data-stu-id="2012e-171">Radio button: `<input type="radio">`</span></span>
- <span data-ttu-id="2012e-172">Botão: `<input type="button">`</span><span class="sxs-lookup"><span data-stu-id="2012e-172">Button: `<input type="button">`</span></span>
- <span data-ttu-id="2012e-173">Botão enviar: `<input type="submit">`</span><span class="sxs-lookup"><span data-stu-id="2012e-173">Submit button: `<input type="submit">`</span></span>

<span data-ttu-id="2012e-174">Você também pode usar o elemento `<textarea>` para criar uma caixa de texto de várias linhas e o elemento `<select>` para criar uma lista suspensa ou lista rolável.</span><span class="sxs-lookup"><span data-stu-id="2012e-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="2012e-175">(Para obter mais informações sobre elementos de formulário HTML, consulte [formulários HTML e entrada](http://www.w3schools.com/html/html_forms.asp) no site W3Schools.)</span><span class="sxs-lookup"><span data-stu-id="2012e-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="2012e-176">O atributo `name` é muito importante, pois o nome é como você obterá o valor do elemento posteriormente, como você verá em breve.</span><span class="sxs-lookup"><span data-stu-id="2012e-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="2012e-177">A parte interessante é o que você, o desenvolvedor de páginas, faz com a entrada do usuário.</span><span class="sxs-lookup"><span data-stu-id="2012e-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="2012e-178">Não há nenhum comportamento interno associado a esses elementos.</span><span class="sxs-lookup"><span data-stu-id="2012e-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="2012e-179">Em vez disso, você precisa obter os valores que o usuário inseriu ou selecionou e faz algo com eles.</span><span class="sxs-lookup"><span data-stu-id="2012e-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="2012e-180">É isso que você aprenderá neste tutorial.</span><span class="sxs-lookup"><span data-stu-id="2012e-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="2012e-181">**HTML5 e formulários de entrada**</span><span class="sxs-lookup"><span data-stu-id="2012e-181">**HTML5 and Input Forms**</span></span>
> 
> <span data-ttu-id="2012e-182">Como você deve saber, o HTML está em transição e a versão mais recente (HTML5) inclui suporte para maneiras mais intuitivas para os usuários inserirem informações.</span><span class="sxs-lookup"><span data-stu-id="2012e-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="2012e-183">Por exemplo, no HTML5, você (o desenvolvedor da página) pode informar à página que você deseja que o usuário insira uma data.</span><span class="sxs-lookup"><span data-stu-id="2012e-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="2012e-184">O navegador pode exibir automaticamente um calendário em vez de exigir que o usuário insira uma data manualmente.</span><span class="sxs-lookup"><span data-stu-id="2012e-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="2012e-185">No entanto, o HTML5 é novo e ainda não tem suporte em todos os navegadores.</span><span class="sxs-lookup"><span data-stu-id="2012e-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="2012e-186">Páginas da Web do ASP.NET dá suporte à entrada HTML5 na medida que o navegador do usuário faz.</span><span class="sxs-lookup"><span data-stu-id="2012e-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="2012e-187">Para obter uma ideia dos novos atributos para o elemento `<input>` no HTML5, consulte [HTML &lt;input&gt; atributo Type](http://www.w3schools.com/html/html_form_input_types.asp) no site W3Schools.</span><span class="sxs-lookup"><span data-stu-id="2012e-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>

## <a name="creating-the-form"></a><span data-ttu-id="2012e-188">Criando o formulário</span><span class="sxs-lookup"><span data-stu-id="2012e-188">Creating the Form</span></span>

<span data-ttu-id="2012e-189">No WebMatrix, no espaço de trabalho **arquivos** , abra a página *Movies. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="2012e-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="2012e-190">Após a marcação de `</h1>` de fechamento e antes da marcação de `<div>` de abertura da chamada `grid.GetHtml`, adicione a seguinte marcação:</span><span class="sxs-lookup"><span data-stu-id="2012e-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="2012e-191">Essa marcação cria um formulário que tem uma caixa de texto chamada `searchGenre` e um botão enviar.</span><span class="sxs-lookup"><span data-stu-id="2012e-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="2012e-192">A caixa de texto e o botão enviar são colocados em um elemento `<form>` cujo atributo `method` está definido como `get`.</span><span class="sxs-lookup"><span data-stu-id="2012e-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="2012e-193">(Lembre-se de que, se você não colocar a caixa de texto e o botão enviar dentro de um elemento `<form>`, nada será enviado quando você clicar no botão.) Você usa o verbo `GET` aqui porque está criando um formulário que não faz nenhuma alteração no servidor — ele simplesmente resulta em uma pesquisa.</span><span class="sxs-lookup"><span data-stu-id="2012e-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="2012e-194">(No tutorial anterior, você usou um método `post`, que é como você envia as alterações para o servidor.</span><span class="sxs-lookup"><span data-stu-id="2012e-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="2012e-195">Você verá isso no próximo tutorial novamente.)</span><span class="sxs-lookup"><span data-stu-id="2012e-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="2012e-196">Execute a página.</span><span class="sxs-lookup"><span data-stu-id="2012e-196">Run the page.</span></span> <span data-ttu-id="2012e-197">Embora você não tenha definido nenhum comportamento para o formulário, você pode ver como ele se parece com:</span><span class="sxs-lookup"><span data-stu-id="2012e-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![Página de filmes com caixa de pesquisa para gênero](form-basics/_static/image3.png)

<span data-ttu-id="2012e-199">Insira um valor na caixa de texto, como "comédia".</span><span class="sxs-lookup"><span data-stu-id="2012e-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="2012e-200">Em seguida, clique em **Pesquisar gênero**.</span><span class="sxs-lookup"><span data-stu-id="2012e-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="2012e-201">Anote a URL da página.</span><span class="sxs-lookup"><span data-stu-id="2012e-201">Take note of the URL of the page.</span></span> <span data-ttu-id="2012e-202">Como você define o atributo de `method` do elemento de `<form>` como `get`, o valor que você inseriu agora é parte da cadeia de caracteres de consulta na URL, desta forma:</span><span class="sxs-lookup"><span data-stu-id="2012e-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="2012e-203">Lendo valores de formulário</span><span class="sxs-lookup"><span data-stu-id="2012e-203">Reading Form Values</span></span>

<span data-ttu-id="2012e-204">A página já contém algum código que obtém os dados do banco e exibe os resultados em uma grade.</span><span class="sxs-lookup"><span data-stu-id="2012e-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="2012e-205">Agora, você precisa adicionar algum código que leia o valor da caixa de texto para que você possa executar uma consulta SQL que inclui o termo de pesquisa.</span><span class="sxs-lookup"><span data-stu-id="2012e-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="2012e-206">Como você define o método do formulário como `get`, você pode ler o valor que foi inserido na caixa de texto usando um código semelhante ao seguinte:</span><span class="sxs-lookup"><span data-stu-id="2012e-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="2012e-207">O objeto `Request.QueryString` (a propriedade `QueryString` do objeto `Request`) inclui os valores de elementos que foram enviados como parte da operação de `GET`.</span><span class="sxs-lookup"><span data-stu-id="2012e-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="2012e-208">A propriedade `Request.QueryString` contém uma *coleção* (uma lista) dos valores que são enviados no formulário.</span><span class="sxs-lookup"><span data-stu-id="2012e-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="2012e-209">Para obter qualquer valor individual, especifique o nome do elemento desejado.</span><span class="sxs-lookup"><span data-stu-id="2012e-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="2012e-210">É por isso que você precisa ter um atributo `name` no elemento `<input>` (`searchTerm`) que cria a caixa de texto.</span><span class="sxs-lookup"><span data-stu-id="2012e-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="2012e-211">(Para obter mais informações sobre o objeto `Request`, consulte a [barra lateral](#BKMK_TheRequestObject) mais tarde.)</span><span class="sxs-lookup"><span data-stu-id="2012e-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="2012e-212">É simples o suficiente para ler o valor da caixa de texto.</span><span class="sxs-lookup"><span data-stu-id="2012e-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="2012e-213">Mas se o usuário não inseriu nada na caixa de texto, mas clicou em **Pesquisar** mesmo assim, você pode ignorar esse clique, já que não há nada a ser pesquisado.</span><span class="sxs-lookup"><span data-stu-id="2012e-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="2012e-214">O código a seguir é um exemplo que mostra como implementar essas condições.</span><span class="sxs-lookup"><span data-stu-id="2012e-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="2012e-215">(Você não precisa adicionar esse código ainda; você fará isso daqui a pouco.)</span><span class="sxs-lookup"><span data-stu-id="2012e-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="2012e-216">O teste é dividido desta forma:</span><span class="sxs-lookup"><span data-stu-id="2012e-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="2012e-217">Obtenha o valor de `Request.QueryString["searchGenre"]`, ou seja, o valor que foi inserido no elemento `<input>` chamado `searchGenre`.</span><span class="sxs-lookup"><span data-stu-id="2012e-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="2012e-218">Descubra se ele está vazio usando o método `IsEmpty`.</span><span class="sxs-lookup"><span data-stu-id="2012e-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="2012e-219">Esse método é a maneira padrão de determinar se algo (por exemplo, um elemento form) contém um valor.</span><span class="sxs-lookup"><span data-stu-id="2012e-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="2012e-220">Mas, na verdade, você se preocupa apenas se *não* estiver vazio, portanto...</span><span class="sxs-lookup"><span data-stu-id="2012e-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="2012e-221">Adicione o operador de `!` na frente do teste de `IsEmpty`.</span><span class="sxs-lookup"><span data-stu-id="2012e-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="2012e-222">(O operador de `!` significa lógico não).</span><span class="sxs-lookup"><span data-stu-id="2012e-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="2012e-223">Em inglês simples, a condição de `if` inteira se traduz no seguinte: *se o elemento searchGenre do formulário não estiver vazio, então.* ..</span><span class="sxs-lookup"><span data-stu-id="2012e-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="2012e-224">Esse bloco define o estágio para criar uma consulta que usa o termo de pesquisa.</span><span class="sxs-lookup"><span data-stu-id="2012e-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="2012e-225">Você fará isso na próxima seção.</span><span class="sxs-lookup"><span data-stu-id="2012e-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> <span data-ttu-id="2012e-226">**O objeto de solicitação**</span><span class="sxs-lookup"><span data-stu-id="2012e-226">**The Request Object**</span></span>
> 
> <span data-ttu-id="2012e-227">O objeto `Request` contém todas as informações que o navegador envia para seu aplicativo quando uma página é solicitada ou enviada.</span><span class="sxs-lookup"><span data-stu-id="2012e-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="2012e-228">Esse objeto inclui todas as informações que o usuário fornece, como valores de caixa de texto ou um arquivo para carregar.</span><span class="sxs-lookup"><span data-stu-id="2012e-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="2012e-229">Ele também inclui todos os tipos de informações adicionais, como cookies, valores na cadeia de caracteres de consulta de URL (se houver), o caminho do arquivo da página em execução, o tipo de navegador que o usuário está usando, a lista de idiomas que estão definidos no navegador e muito mais.</span><span class="sxs-lookup"><span data-stu-id="2012e-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="2012e-230">O objeto `Request` é uma *coleção* (lista) de valores.</span><span class="sxs-lookup"><span data-stu-id="2012e-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="2012e-231">Você Obtém um valor individual da coleção especificando seu nome:</span><span class="sxs-lookup"><span data-stu-id="2012e-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="2012e-232">O objeto `Request` realmente expõe vários subconjuntos.</span><span class="sxs-lookup"><span data-stu-id="2012e-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="2012e-233">Por exemplo:</span><span class="sxs-lookup"><span data-stu-id="2012e-233">For example:</span></span>
> 
> - <span data-ttu-id="2012e-234">`Request.Form` fornece valores de elementos dentro do elemento `<form>` enviado se a solicitação for uma solicitação de `POST`.</span><span class="sxs-lookup"><span data-stu-id="2012e-234">`Request.Form` gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - <span data-ttu-id="2012e-235">`Request.QueryString` fornece apenas os valores na cadeia de caracteres de consulta da URL.</span><span class="sxs-lookup"><span data-stu-id="2012e-235">`Request.QueryString` gives you just the values in the URL's query string.</span></span> <span data-ttu-id="2012e-236">(Em uma URL como `http://mysite/myapp/page?searchGenre=action&page=2`, a seção `?searchGenre=action&page=2` da URL é a cadeia de caracteres de consulta.)</span><span class="sxs-lookup"><span data-stu-id="2012e-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - <span data-ttu-id="2012e-237">`Request.Cookies` coleção fornece acesso a cookies que o navegador enviou.</span><span class="sxs-lookup"><span data-stu-id="2012e-237">`Request.Cookies` collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="2012e-238">Para obter um valor que você sabe que está no formulário enviado, você pode usar `Request["name"]`.</span><span class="sxs-lookup"><span data-stu-id="2012e-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="2012e-239">Como alternativa, você pode usar as versões mais específicas `Request.Form["name"]` (para solicitações de `POST`) ou `Request.QueryString["name"]` (para solicitações de `GET`).</span><span class="sxs-lookup"><span data-stu-id="2012e-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="2012e-240">É claro que *Name* é o nome do item a ser obtido.</span><span class="sxs-lookup"><span data-stu-id="2012e-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="2012e-241">O nome do item que você deseja obter deve ser exclusivo na coleção que você está usando.</span><span class="sxs-lookup"><span data-stu-id="2012e-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="2012e-242">É por isso que o objeto `Request` fornece os subconjuntos como `Request.Form` e `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="2012e-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="2012e-243">Suponha que sua página contenha um elemento Form chamado `userName` e *também* contenha um cookie chamado `userName`.</span><span class="sxs-lookup"><span data-stu-id="2012e-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="2012e-244">Se você receber `Request["userName"]`, será ambíguo se você quiser o valor do formulário ou o cookie.</span><span class="sxs-lookup"><span data-stu-id="2012e-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="2012e-245">No entanto, se você obter `Request.Form["userName"]` ou `Request.Cookie["userName"]`, você está sendo explícito sobre qual valor obter.</span><span class="sxs-lookup"><span data-stu-id="2012e-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="2012e-246">É uma boa prática ser específica e usar o subconjunto de `Request` em que você está interessado, como `Request.Form` ou `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="2012e-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="2012e-247">Para as páginas simples que você está criando neste tutorial, ele provavelmente não faz nenhuma diferença.</span><span class="sxs-lookup"><span data-stu-id="2012e-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="2012e-248">No entanto, à medida que você cria páginas mais complexas, usar a versão explícita `Request.Form` ou `Request.QueryString` pode ajudá-lo a evitar problemas que podem surgir quando a página contém um formulário (ou vários formulários), cookies, valores de cadeia de caracteres de consulta e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="2012e-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>

## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="2012e-249">Criando uma consulta usando um termo de pesquisa</span><span class="sxs-lookup"><span data-stu-id="2012e-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="2012e-250">Agora que você sabe como obter o termo de pesquisa que o usuário inseriu, você pode criar uma consulta que o utiliza.</span><span class="sxs-lookup"><span data-stu-id="2012e-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="2012e-251">Lembre-se de que, para obter todos os itens de filme do banco de dados, você está usando uma consulta SQL parecida com esta instrução:</span><span class="sxs-lookup"><span data-stu-id="2012e-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="2012e-252">Para obter apenas alguns filmes, você precisa usar uma consulta que inclua uma cláusula `Where`.</span><span class="sxs-lookup"><span data-stu-id="2012e-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="2012e-253">Essa cláusula permite definir uma condição na qual as linhas são retornadas pela consulta.</span><span class="sxs-lookup"><span data-stu-id="2012e-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="2012e-254">Aqui está um exemplo:</span><span class="sxs-lookup"><span data-stu-id="2012e-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="2012e-255">O formato básico é `WHERE column = value`.</span><span class="sxs-lookup"><span data-stu-id="2012e-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="2012e-256">Você pode usar operadores diferentes, além de apenas `=`, como `>` (maior que), `<` (menor que), `<>` (diferente de), `<=` (menor ou igual a), etc., dependendo do que você está procurando.</span><span class="sxs-lookup"><span data-stu-id="2012e-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="2012e-257">Caso você esteja imaginando, as instruções SQL não diferenciam maiúsculas de minúsculas &mdash; `SELECT` é o mesmo que `Select` (ou até mesmo `select`).</span><span class="sxs-lookup"><span data-stu-id="2012e-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="2012e-258">No entanto, as pessoas frequentemente capitalizam palavras-chave em uma instrução SQL, como `SELECT` e `WHERE`, para facilitar a leitura.</span><span class="sxs-lookup"><span data-stu-id="2012e-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="2012e-259">Passando o termo de pesquisa como um parâmetro</span><span class="sxs-lookup"><span data-stu-id="2012e-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="2012e-260">Pesquisar por um gênero específico é fácil o suficiente (`WHERE Genre = 'Action'`), mas você deseja ser capaz de pesquisar qualquer gênero que o usuário inserir.</span><span class="sxs-lookup"><span data-stu-id="2012e-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="2012e-261">Para fazer isso, você cria como consulta SQL que inclui um espaço reservado para o valor a ser pesquisado.</span><span class="sxs-lookup"><span data-stu-id="2012e-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="2012e-262">Ele se parecerá com este comando:</span><span class="sxs-lookup"><span data-stu-id="2012e-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="2012e-263">O espaço reservado é o `@` caractere seguido de zero.</span><span class="sxs-lookup"><span data-stu-id="2012e-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="2012e-264">Como você pode imaginar, uma consulta pode conter vários espaços reservados e ele será nomeado `@0`, `@1`, `@2`, etc.</span><span class="sxs-lookup"><span data-stu-id="2012e-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="2012e-265">Para configurar a consulta e, na verdade, passá-la para o valor, use o código como o seguinte:</span><span class="sxs-lookup"><span data-stu-id="2012e-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="2012e-266">Esse código é semelhante ao que você já fez para exibir dados na grade.</span><span class="sxs-lookup"><span data-stu-id="2012e-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="2012e-267">As únicas diferenças são:</span><span class="sxs-lookup"><span data-stu-id="2012e-267">The only differences are:</span></span>

- <span data-ttu-id="2012e-268">A consulta contém um espaço reservado (`WHERE Genre = @0"`).</span><span class="sxs-lookup"><span data-stu-id="2012e-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="2012e-269">A consulta é colocada em uma variável (`selectCommand`); antes, você passou a consulta diretamente para o método `db.Query`.</span><span class="sxs-lookup"><span data-stu-id="2012e-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="2012e-270">Ao chamar o método `db.Query`, você passa a consulta e o valor a ser usado para o espaço reservado.</span><span class="sxs-lookup"><span data-stu-id="2012e-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="2012e-271">(Se a consulta tivesse vários espaços reservados, você os passaria como valores separados para o método.)</span><span class="sxs-lookup"><span data-stu-id="2012e-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="2012e-272">Se você colocar todos esses elementos juntos, obterá o seguinte código:</span><span class="sxs-lookup"><span data-stu-id="2012e-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> <span data-ttu-id="2012e-273">**Importante!**</span><span class="sxs-lookup"><span data-stu-id="2012e-273">**Important!**</span></span> <span data-ttu-id="2012e-274">O uso de espaços reservados (como `@0`) para passar valores para um comando SQL é *extremamente importante* para a segurança.</span><span class="sxs-lookup"><span data-stu-id="2012e-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="2012e-275">A maneira como você o vê aqui, com espaços reservados para dados variáveis, é a única maneira de construir comandos SQL.</span><span class="sxs-lookup"><span data-stu-id="2012e-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="2012e-276">Nunca Construa uma instrução SQL reunindo (concatenando) o texto literal e os valores obtidos do usuário.</span><span class="sxs-lookup"><span data-stu-id="2012e-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="2012e-277">Concatenar a entrada do usuário em uma instrução SQL abre seu site para um *ataque de injeção de SQL* em que um usuário mal-intencionado envia valores para sua página que atacam seu banco de dados.</span><span class="sxs-lookup"><span data-stu-id="2012e-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="2012e-278">(Você pode ler mais no artigo [injeção de SQL](https://msdn.microsoft.com/library/ms161953.aspx) no site do MSDN.)</span><span class="sxs-lookup"><span data-stu-id="2012e-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>

## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="2012e-279">Atualizando a página de filmes com o código de pesquisa</span><span class="sxs-lookup"><span data-stu-id="2012e-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="2012e-280">Agora você pode atualizar o código no arquivo *Movies. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="2012e-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="2012e-281">Para começar, substitua o código no bloco de código na parte superior da página por este código:</span><span class="sxs-lookup"><span data-stu-id="2012e-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="2012e-282">A diferença aqui é que você colocou a consulta na variável `selectCommand`, que você passará para `db.Query` mais tarde.</span><span class="sxs-lookup"><span data-stu-id="2012e-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="2012e-283">Colocar a instrução SQL em uma variável permite que você altere a instrução, que é o que você fará para executar a pesquisa.</span><span class="sxs-lookup"><span data-stu-id="2012e-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="2012e-284">Você também removeu essas duas linhas, as quais você colocará de volta mais tarde:</span><span class="sxs-lookup"><span data-stu-id="2012e-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="2012e-285">Você não deseja executar a consulta ainda (ou seja, chamar `db.Query`) e não quer inicializar o auxiliar de `WebGrid` ainda.</span><span class="sxs-lookup"><span data-stu-id="2012e-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="2012e-286">Você fará essas coisas depois de descobrir qual instrução SQL precisa ser executada.</span><span class="sxs-lookup"><span data-stu-id="2012e-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="2012e-287">Após esse bloco reescrito, você pode adicionar a nova lógica para manipular a pesquisa.</span><span class="sxs-lookup"><span data-stu-id="2012e-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="2012e-288">O código completo se parecerá com o seguinte.</span><span class="sxs-lookup"><span data-stu-id="2012e-288">The completed code will look like the following.</span></span> <span data-ttu-id="2012e-289">Atualize o código em sua página para que ele corresponda a este exemplo:</span><span class="sxs-lookup"><span data-stu-id="2012e-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="2012e-290">A página agora funciona como esta.</span><span class="sxs-lookup"><span data-stu-id="2012e-290">The page now works like this.</span></span> <span data-ttu-id="2012e-291">Toda vez que a página é executada, o código abre o banco de dados e a variável `selectCommand` é definida como a instrução SQL que obtém todos os registros da tabela `Movies`.</span><span class="sxs-lookup"><span data-stu-id="2012e-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="2012e-292">O código também inicializa a variável `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="2012e-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="2012e-293">No entanto, se a solicitação atual incluir um valor para o elemento `searchGenre`, o código definirá `selectCommand` para uma consulta diferente — ou seja, para um que inclua a cláusula `Where` para pesquisar um gênero.</span><span class="sxs-lookup"><span data-stu-id="2012e-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="2012e-294">Ele também define `searchTerm` como o que foi passado para a caixa de pesquisa (que pode ser Nothing).</span><span class="sxs-lookup"><span data-stu-id="2012e-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="2012e-295">Independentemente de qual instrução SQL está em `selectCommand`, o código então chama `db.Query` para executar a consulta, passando o que estiver em `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="2012e-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="2012e-296">Se não houver nada em `searchTerm`, não importa, porque nesse caso não há nenhum parâmetro para passar o valor para `selectCommand` mesmo assim.</span><span class="sxs-lookup"><span data-stu-id="2012e-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="2012e-297">Por fim, o código inicializa o `WebGrid` auxiliar usando os resultados da consulta, assim como antes.</span><span class="sxs-lookup"><span data-stu-id="2012e-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="2012e-298">Você pode ver que, colocando a instrução SQL e o termo de pesquisa em variáveis, você adicionou flexibilidade ao código.</span><span class="sxs-lookup"><span data-stu-id="2012e-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="2012e-299">Como você verá mais adiante neste tutorial, você pode usar essa estrutura básica e continuar adicionando lógica para diferentes tipos de pesquisas.</span><span class="sxs-lookup"><span data-stu-id="2012e-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="2012e-300">Testando o recurso Pesquisar por gênero</span><span class="sxs-lookup"><span data-stu-id="2012e-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="2012e-301">No WebMatrix, execute a página *Movies. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="2012e-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="2012e-302">Você vê a página com a caixa de texto para gênero.</span><span class="sxs-lookup"><span data-stu-id="2012e-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="2012e-303">Insira um gênero que você inseriu para um dos seus registros de teste e clique em **Pesquisar**.</span><span class="sxs-lookup"><span data-stu-id="2012e-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="2012e-304">Desta vez, você verá uma listagem apenas dos filmes que correspondem a esse gênero:</span><span class="sxs-lookup"><span data-stu-id="2012e-304">This time you see a listing of just the movies that match that genre:</span></span>

![Listagem de filmes página depois de procurar o gênero ' Comedies '](form-basics/_static/image4.png)

<span data-ttu-id="2012e-306">Insira um gênero diferente e pesquise novamente.</span><span class="sxs-lookup"><span data-stu-id="2012e-306">Enter a different genre and search again.</span></span> <span data-ttu-id="2012e-307">Tente inserir o gênero usando todas as letras maiúsculas ou minúsculas para que você possa ver que a pesquisa não diferencia maiúsculas de minúsculas.</span><span class="sxs-lookup"><span data-stu-id="2012e-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="2012e-308">"Lembrando" o que o usuário inseriu</span><span class="sxs-lookup"><span data-stu-id="2012e-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="2012e-309">Você deve ter notado que depois de inserir um gênero e clicar em **Pesquisar gênero**, você viu uma listagem para esse gênero.</span><span class="sxs-lookup"><span data-stu-id="2012e-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="2012e-310">No entanto, a caixa de texto de pesquisa estava vazia &mdash; em outras palavras, não se lembra do que você inseriu.</span><span class="sxs-lookup"><span data-stu-id="2012e-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="2012e-311">É importante entender por que esse comportamento ocorre.</span><span class="sxs-lookup"><span data-stu-id="2012e-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="2012e-312">Quando você envia uma página, o navegador envia uma solicitação ao servidor Web.</span><span class="sxs-lookup"><span data-stu-id="2012e-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="2012e-313">Quando o ASP.NET obtém a solicitação, ele cria uma instância totalmente nova da página, executa o código nela e, em seguida, renderiza a página para o navegador novamente.</span><span class="sxs-lookup"><span data-stu-id="2012e-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="2012e-314">No entanto, na verdade, a página não sabe que você estava apenas trabalhando com uma versão anterior de si mesma.</span><span class="sxs-lookup"><span data-stu-id="2012e-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="2012e-315">Tudo o que ele sabe é que ele recebeu uma solicitação que tinha alguns dados de formulário.</span><span class="sxs-lookup"><span data-stu-id="2012e-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="2012e-316">Sempre que você solicitar uma página &mdash; se pela primeira vez ou enviando-a &mdash; você está obtendo uma nova página.</span><span class="sxs-lookup"><span data-stu-id="2012e-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="2012e-317">O servidor Web não tem memória de sua última solicitação.</span><span class="sxs-lookup"><span data-stu-id="2012e-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="2012e-318">Nenhuma das ASP.NET, e nenhuma delas faz o navegador.</span><span class="sxs-lookup"><span data-stu-id="2012e-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="2012e-319">A única conexão entre essas instâncias separadas da página é qualquer dado que você transmite entre elas.</span><span class="sxs-lookup"><span data-stu-id="2012e-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="2012e-320">Se você enviar uma página, por exemplo, a nova instância de página poderá obter os dados do formulário que foram enviados pela instância anterior.</span><span class="sxs-lookup"><span data-stu-id="2012e-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="2012e-321">(Outra maneira de passar dados entre páginas é usar cookies.)</span><span class="sxs-lookup"><span data-stu-id="2012e-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="2012e-322">Uma maneira formal de descrever essa situação é dizer que as páginas da Web são *sem estado*.</span><span class="sxs-lookup"><span data-stu-id="2012e-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="2012e-323">Os servidores Web e as próprias páginas e os elementos na página não mantêm nenhuma informação sobre o estado anterior de uma página.</span><span class="sxs-lookup"><span data-stu-id="2012e-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="2012e-324">A Web foi projetada dessa forma porque manter o estado das solicitações individuais esgotaria rapidamente os recursos dos servidores Web, que geralmente lidam com milhares, talvez até centenas de milhares de solicitações por segundo.</span><span class="sxs-lookup"><span data-stu-id="2012e-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="2012e-325">É por isso que a caixa de texto estava vazia.</span><span class="sxs-lookup"><span data-stu-id="2012e-325">So that's why the text box was empty.</span></span> <span data-ttu-id="2012e-326">Depois de enviar a página, o ASP.NET criou uma nova instância da página e executou o código e a marcação.</span><span class="sxs-lookup"><span data-stu-id="2012e-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="2012e-327">Não havia nada no código que disse ASP.NET para colocar um valor na caixa de texto.</span><span class="sxs-lookup"><span data-stu-id="2012e-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="2012e-328">Portanto, o ASP.NET não fez nada e a caixa de texto foi renderizada sem um valor nele.</span><span class="sxs-lookup"><span data-stu-id="2012e-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="2012e-329">Na verdade, há uma maneira fácil de contornar esse problema.</span><span class="sxs-lookup"><span data-stu-id="2012e-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="2012e-330">O gênero que você inseriu na caixa de texto *está* disponível no código &mdash; ele está em `Request.QueryString["searchGenre"]`.</span><span class="sxs-lookup"><span data-stu-id="2012e-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="2012e-331">Atualize a marcação da caixa de texto para que o atributo `value` Obtenha seu valor de `searchTerm`, como neste exemplo:</span><span class="sxs-lookup"><span data-stu-id="2012e-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="2012e-332">Nesta página, você também poderia definir o atributo `value` como a variável `searchTerm`, já que essa variável também contém o gênero que você inseriu.</span><span class="sxs-lookup"><span data-stu-id="2012e-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="2012e-333">Mas usar o objeto `Request` para definir o atributo `value`, como mostrado aqui é a maneira padrão de realizar essa tarefa.</span><span class="sxs-lookup"><span data-stu-id="2012e-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="2012e-334">(Supondo que você ainda deseja fazer isso &mdash; em algumas situações, talvez você queira renderizar a página *sem* valores nos campos.</span><span class="sxs-lookup"><span data-stu-id="2012e-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="2012e-335">Tudo depende do que está acontecendo com seu aplicativo.)</span><span class="sxs-lookup"><span data-stu-id="2012e-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="2012e-336">Não é possível "lembrar" o valor de uma caixa de texto usada para senhas.</span><span class="sxs-lookup"><span data-stu-id="2012e-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="2012e-337">Seria uma brecha de segurança para permitir que as pessoas preencham um campo de senha usando código.</span><span class="sxs-lookup"><span data-stu-id="2012e-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>

<span data-ttu-id="2012e-338">Execute a página novamente, insira um gênero e clique em **Pesquisar gênero**.</span><span class="sxs-lookup"><span data-stu-id="2012e-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="2012e-339">Dessa vez, não só você vê os resultados da pesquisa, mas a caixa de texto lembra o que você inseriu pela última vez:</span><span class="sxs-lookup"><span data-stu-id="2012e-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![Página mostrando que a caixa de texto tem ' lembrado ' da entrada anterior](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="2012e-341">Pesquisando qualquer palavra no título</span><span class="sxs-lookup"><span data-stu-id="2012e-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="2012e-342">Agora você pode pesquisar por qualquer gênero, mas também convém procurar um título.</span><span class="sxs-lookup"><span data-stu-id="2012e-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="2012e-343">É difícil obter um título exatamente à direita ao pesquisar, então você pode pesquisar uma palavra que aparece em qualquer lugar dentro de um título.</span><span class="sxs-lookup"><span data-stu-id="2012e-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="2012e-344">Para fazer isso no SQL, use o operador de `LIKE` e a sintaxe como a seguinte:</span><span class="sxs-lookup"><span data-stu-id="2012e-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="2012e-345">Esse comando obtém todos os filmes cujos títulos contêm "Adventure".</span><span class="sxs-lookup"><span data-stu-id="2012e-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="2012e-346">Ao usar o operador de `LIKE`, você inclui o caractere curinga `%` como parte do termo de pesquisa.</span><span class="sxs-lookup"><span data-stu-id="2012e-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="2012e-347">A pesquisa `LIKE 'adventure%'` significa "começando com ' Adventure '".</span><span class="sxs-lookup"><span data-stu-id="2012e-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="2012e-348">(Tecnicamente, isso significa "a cadeia de caracteres ' aventura ' seguida de qualquer coisa.") Da mesma forma, o termo de pesquisa `LIKE '%adventure'` significa "qualquer coisa seguida pela cadeia de caracteres ' Adventure '", que é outra maneira de dizer "terminando com ' Adventure '".</span><span class="sxs-lookup"><span data-stu-id="2012e-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="2012e-349">O termo de pesquisa `LIKE '%adventure%'`, portanto, significa "com ' Adventure ' em qualquer lugar no título."</span><span class="sxs-lookup"><span data-stu-id="2012e-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="2012e-350">(Tecnicamente, "qualquer coisa no título, seguido de ' Adventure ', seguido de qualquer coisa.")</span><span class="sxs-lookup"><span data-stu-id="2012e-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="2012e-351">Dentro do elemento `<form>`, adicione a seguinte marcação logo abaixo da marca `</div>` de fechamento para a pesquisa de gênero (logo antes do elemento `</form>` de fechamento):</span><span class="sxs-lookup"><span data-stu-id="2012e-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="2012e-352">O código para lidar com essa pesquisa é semelhante ao código da pesquisa de gênero, exceto pelo fato de que você precisa montar a pesquisa de `LIKE`.</span><span class="sxs-lookup"><span data-stu-id="2012e-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="2012e-353">Dentro do bloco de código na parte superior da página, adicione esse `if` bloco logo após o bloco de `if` para a pesquisa de Gênero:</span><span class="sxs-lookup"><span data-stu-id="2012e-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="2012e-354">Esse código usa a mesma lógica que você viu anteriormente, exceto que a pesquisa usa um operador de `LIKE` e o código coloca "`%`" antes e depois do termo de pesquisa.</span><span class="sxs-lookup"><span data-stu-id="2012e-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="2012e-355">Observe como é fácil adicionar outra pesquisa à página.</span><span class="sxs-lookup"><span data-stu-id="2012e-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="2012e-356">Tudo o que você precisava fazer era:</span><span class="sxs-lookup"><span data-stu-id="2012e-356">All you had to do was:</span></span>

- <span data-ttu-id="2012e-357">Crie um bloco de `if` que foi testado para ver se a caixa de pesquisa relevante tinha um valor.</span><span class="sxs-lookup"><span data-stu-id="2012e-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="2012e-358">Defina a variável `selectCommand` como uma nova instrução SQL.</span><span class="sxs-lookup"><span data-stu-id="2012e-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="2012e-359">Defina a variável `searchTerm` como o valor a ser passado para a consulta.</span><span class="sxs-lookup"><span data-stu-id="2012e-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="2012e-360">Aqui está o bloco de código completo, que contém a nova lógica para uma pesquisa de título:</span><span class="sxs-lookup"><span data-stu-id="2012e-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="2012e-361">Aqui está um resumo do que esse código faz:</span><span class="sxs-lookup"><span data-stu-id="2012e-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="2012e-362">As variáveis `searchTerm` e `selectCommand` são inicializadas na parte superior.</span><span class="sxs-lookup"><span data-stu-id="2012e-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="2012e-363">Você vai definir essas variáveis para o termo de pesquisa apropriado (se houver) e o comando SQL apropriado com base no que o usuário faz na página.</span><span class="sxs-lookup"><span data-stu-id="2012e-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="2012e-364">A pesquisa padrão é o caso simples de obter todos os filmes do banco de dados.</span><span class="sxs-lookup"><span data-stu-id="2012e-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="2012e-365">Nos testes para `searchGenre` e `searchTitle`, o código define `searchTerm` para o valor que você deseja pesquisar.</span><span class="sxs-lookup"><span data-stu-id="2012e-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="2012e-366">Esses blocos de código também definem `selectCommand` para um comando SQL apropriado para essa pesquisa.</span><span class="sxs-lookup"><span data-stu-id="2012e-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="2012e-367">O método `db.Query` é invocado apenas uma vez, usando qualquer comando SQL que esteja em `selectedCommand` e qualquer valor que esteja em `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="2012e-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="2012e-368">Se não houver nenhum termo de pesquisa (sem gênero e nenhuma palavra de título), o valor de `searchTerm` será uma cadeia de caracteres vazia.</span><span class="sxs-lookup"><span data-stu-id="2012e-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="2012e-369">No entanto, isso não importa, porque nesse caso a consulta não requer um parâmetro.</span><span class="sxs-lookup"><span data-stu-id="2012e-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="2012e-370">Testando o recurso de pesquisa de título</span><span class="sxs-lookup"><span data-stu-id="2012e-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="2012e-371">Agora você pode testar a página de pesquisa concluída.</span><span class="sxs-lookup"><span data-stu-id="2012e-371">Now you can test your completed search page.</span></span> <span data-ttu-id="2012e-372">Execute *Movies. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="2012e-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="2012e-373">Insira um gênero e clique em **Pesquisar gênero**.</span><span class="sxs-lookup"><span data-stu-id="2012e-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="2012e-374">A grade exibe filmes desse gênero, como antes.</span><span class="sxs-lookup"><span data-stu-id="2012e-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="2012e-375">Insira uma palavra de título e clique em **título de pesquisa**.</span><span class="sxs-lookup"><span data-stu-id="2012e-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="2012e-376">A grade exibe os filmes que têm essa palavra no título.</span><span class="sxs-lookup"><span data-stu-id="2012e-376">The grid displays movies that have that word in the title.</span></span>

![Página de filmes listando depois de procurar ' o ' no título](form-basics/_static/image6.png)

<span data-ttu-id="2012e-378">Deixe ambas as caixas de texto em branco e clique em um dos botões.</span><span class="sxs-lookup"><span data-stu-id="2012e-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="2012e-379">A grade exibe todos os filmes.</span><span class="sxs-lookup"><span data-stu-id="2012e-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="2012e-380">Combinando as consultas</span><span class="sxs-lookup"><span data-stu-id="2012e-380">Combining the Queries</span></span>

<span data-ttu-id="2012e-381">Você pode observar que as pesquisas que você pode executar são exclusivas.</span><span class="sxs-lookup"><span data-stu-id="2012e-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="2012e-382">Não é possível pesquisar o título e o gênero ao mesmo tempo, mesmo se ambas as caixas de pesquisa tiverem valores.</span><span class="sxs-lookup"><span data-stu-id="2012e-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="2012e-383">Por exemplo, você não pode pesquisar por todos os filmes de ação cujo título contenha "Adventure".</span><span class="sxs-lookup"><span data-stu-id="2012e-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="2012e-384">(Como a página é codificada agora, se você inserir valores para o gênero e o título, a pesquisa de título obterá precedência.) Para criar uma pesquisa que combine as condições, você precisaria criar uma consulta SQL que tenha sintaxe semelhante à seguinte:</span><span class="sxs-lookup"><span data-stu-id="2012e-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="2012e-385">E você precisaria executar a consulta usando uma instrução como a seguinte (aproximadamente falando):</span><span class="sxs-lookup"><span data-stu-id="2012e-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="2012e-386">A criação de lógica para permitir muitas permutações de critérios de pesquisa pode ficar um pouco envolvida, como você pode ver.</span><span class="sxs-lookup"><span data-stu-id="2012e-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="2012e-387">Portanto, vamos parar aqui.</span><span class="sxs-lookup"><span data-stu-id="2012e-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="2012e-388">Chegando em seguida</span><span class="sxs-lookup"><span data-stu-id="2012e-388">Coming Up Next</span></span>

<span data-ttu-id="2012e-389">No próximo tutorial, você criará uma página que usa um formulário para permitir que os usuários adicionem filmes ao banco de dados.</span><span class="sxs-lookup"><span data-stu-id="2012e-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="2012e-390">Listagem completa da página de filme (atualizada com pesquisa)</span><span class="sxs-lookup"><span data-stu-id="2012e-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="2012e-391">Recursos adicionais</span><span class="sxs-lookup"><span data-stu-id="2012e-391">Additional Resources</span></span>

- [<span data-ttu-id="2012e-392">Introdução à programação da Web do ASP.NET usando a sintaxe do Razor</span><span class="sxs-lookup"><span data-stu-id="2012e-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="2012e-393">[Cláusula WHERE do SQL](http://www.w3schools.com/sql/sql_where.asp) no site W3Schools</span><span class="sxs-lookup"><span data-stu-id="2012e-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="2012e-394">Artigo [definições de método](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) no site W3C</span><span class="sxs-lookup"><span data-stu-id="2012e-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="2012e-395">[Anterior](displaying-data.md)
> [Próximo](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="2012e-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
