---
uid: mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-vb
title: Noções básicas sobre filtros de ação (VB) | Microsoft Docs
author: microsoft
description: O objetivo deste tutorial é explicar os filtros de ação. Um filtro de ação é um atributo que você pode aplicar a uma ação do controlador, ou a um controlador inteiro...
ms.author: riande
ms.date: 10/16/2008
ms.assetid: e83812f2-c53e-4a43-a7c1-d64c59ecf694
msc.legacyurl: /mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-vb
msc.type: authoredcontent
ms.openlocfilehash: 263658231ccaa7863508c691a3570bc00b9e8039
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 03/06/2020
ms.locfileid: "78601167"
---
# <a name="understanding-action-filters-vb"></a><span data-ttu-id="b643d-104">Noções básicas sobre filtros de ação (VB)</span><span class="sxs-lookup"><span data-stu-id="b643d-104">Understanding Action Filters (VB)</span></span>

<span data-ttu-id="b643d-105">pela [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="b643d-105">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="b643d-106">Baixar PDF</span><span class="sxs-lookup"><span data-stu-id="b643d-106">Download PDF</span></span>](https://download.microsoft.com/download/e/f/3/ef3f2ff6-7424-48f7-bdaa-180ef64c3490/ASPNET_MVC_Tutorial_14_VB.pdf)

> <span data-ttu-id="b643d-107">O objetivo deste tutorial é explicar os filtros de ação.</span><span class="sxs-lookup"><span data-stu-id="b643d-107">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="b643d-108">Um filtro de ação é um atributo que você pode aplicar a uma ação de controlador, ou a um controlador inteiro, que modifica a maneira como a ação é executada.</span><span class="sxs-lookup"><span data-stu-id="b643d-108">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span>

## <a name="understanding-action-filters"></a><span data-ttu-id="b643d-109">Noções básicas sobre filtros de ação</span><span class="sxs-lookup"><span data-stu-id="b643d-109">Understanding Action Filters</span></span>

<span data-ttu-id="b643d-110">O objetivo deste tutorial é explicar os filtros de ação.</span><span class="sxs-lookup"><span data-stu-id="b643d-110">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="b643d-111">Um filtro de ação é um atributo que você pode aplicar a uma ação de controlador, ou a um controlador inteiro, que modifica a maneira como a ação é executada.</span><span class="sxs-lookup"><span data-stu-id="b643d-111">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span> <span data-ttu-id="b643d-112">A estrutura MVC do ASP.NET inclui vários filtros de ação:</span><span class="sxs-lookup"><span data-stu-id="b643d-112">The ASP.NET MVC framework includes several action filters:</span></span>

- <span data-ttu-id="b643d-113">OutputCache – esse filtro de ação armazena em cache a saída de uma ação do controlador por um período de tempo especificado.</span><span class="sxs-lookup"><span data-stu-id="b643d-113">OutputCache – This action filter caches the output of a controller action for a specified amount of time.</span></span>
- <span data-ttu-id="b643d-114">HandleError – esse filtro de ação manipula erros gerados quando uma ação do controlador é executada.</span><span class="sxs-lookup"><span data-stu-id="b643d-114">HandleError – This action filter handles errors raised when a controller action executes.</span></span>
- <span data-ttu-id="b643d-115">Autorizar – este filtro de ação permite restringir o acesso a um determinado usuário ou função.</span><span class="sxs-lookup"><span data-stu-id="b643d-115">Authorize – This action filter enables you to restrict access to a particular user or role.</span></span>

<span data-ttu-id="b643d-116">Você também pode criar seus próprios filtros de ação personalizados.</span><span class="sxs-lookup"><span data-stu-id="b643d-116">You also can create your own custom action filters.</span></span> <span data-ttu-id="b643d-117">Por exemplo, talvez você queira criar um filtro de ação personalizado para implementar um sistema de autenticação personalizado.</span><span class="sxs-lookup"><span data-stu-id="b643d-117">For example, you might want to create a custom action filter in order to implement a custom authentication system.</span></span> <span data-ttu-id="b643d-118">Ou talvez você queira criar um filtro de ação que modifique os dados de exibição retornados por uma ação do controlador.</span><span class="sxs-lookup"><span data-stu-id="b643d-118">Or, you might want to create an action filter that modifies the view data returned by a controller action.</span></span>

<span data-ttu-id="b643d-119">Neste tutorial, você aprenderá a criar um filtro de ação desde o início.</span><span class="sxs-lookup"><span data-stu-id="b643d-119">In this tutorial, you learn how to build an action filter from the ground up.</span></span> <span data-ttu-id="b643d-120">Criamos um filtro de ação de log que registra diferentes estágios do processamento de uma ação na janela de saída do Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="b643d-120">We create a Log action filter that logs different stages of the processing of an action to the Visual Studio Output window.</span></span>

### <a name="using-an-action-filter"></a><span data-ttu-id="b643d-121">Usando um filtro de ação</span><span class="sxs-lookup"><span data-stu-id="b643d-121">Using an Action Filter</span></span>

<span data-ttu-id="b643d-122">Um filtro de ação é um atributo.</span><span class="sxs-lookup"><span data-stu-id="b643d-122">An action filter is an attribute.</span></span> <span data-ttu-id="b643d-123">Você pode aplicar a maioria dos filtros de ação a uma ação de controlador individual ou a um controlador inteiro.</span><span class="sxs-lookup"><span data-stu-id="b643d-123">You can apply most action filters to either an individual controller action or an entire controller.</span></span>

<span data-ttu-id="b643d-124">Por exemplo, o controlador de dados na Listagem 1 expõe uma ação chamada `Index()` que retorna a hora atual.</span><span class="sxs-lookup"><span data-stu-id="b643d-124">For example, the Data controller in Listing 1 exposes an action named `Index()` that returns the current time.</span></span> <span data-ttu-id="b643d-125">Essa ação é decorada com o filtro de ação `OutputCache`.</span><span class="sxs-lookup"><span data-stu-id="b643d-125">This action is decorated with the `OutputCache` action filter.</span></span> <span data-ttu-id="b643d-126">Esse filtro faz com que o valor retornado pela ação seja armazenado em cache por 10 segundos.</span><span class="sxs-lookup"><span data-stu-id="b643d-126">This filter causes the value returned by the action to be cached for 10 seconds.</span></span>

<span data-ttu-id="b643d-127">**Listagem 1 – `Controllers\DataController.vb`**</span><span class="sxs-lookup"><span data-stu-id="b643d-127">**Listing 1 – `Controllers\DataController.vb`**</span></span>

[!code-vb[Main](understanding-action-filters-vb/samples/sample1.vb)]

<span data-ttu-id="b643d-128">Se você invocar repetidamente a ação `Index()` inserindo a URL/Data/Index na barra de endereços do navegador e pressionando o botão atualizar várias vezes, você verá o mesmo tempo por 10 segundos.</span><span class="sxs-lookup"><span data-stu-id="b643d-128">If you repeatedly invoke the `Index()` action by entering the URL /Data/Index into the address bar of your browser and hitting the Refresh button multiple times, then you will see the same time for 10 seconds.</span></span> <span data-ttu-id="b643d-129">A saída da ação de `Index()` é armazenada em cache por 10 segundos (consulte a Figura 1).</span><span class="sxs-lookup"><span data-stu-id="b643d-129">The output of the `Index()` action is cached for 10 seconds (see Figure 1).</span></span>

<span data-ttu-id="b643d-130">[tempo em cache ![](understanding-action-filters-vb/_static/image2.png)](understanding-action-filters-vb/_static/image1.png)</span><span class="sxs-lookup"><span data-stu-id="b643d-130">[![Cached time](understanding-action-filters-vb/_static/image2.png)](understanding-action-filters-vb/_static/image1.png)</span></span>

<span data-ttu-id="b643d-131">**Figura 01**: tempo em cache ([clique para exibir a imagem em tamanho normal](understanding-action-filters-vb/_static/image3.png))</span><span class="sxs-lookup"><span data-stu-id="b643d-131">**Figure 01**: Cached time ([Click to view full-size image](understanding-action-filters-vb/_static/image3.png))</span></span>

<span data-ttu-id="b643d-132">Na Listagem 1, um único filtro de ação – o `OutputCache` filtro de ação – é aplicado ao método `Index()`.</span><span class="sxs-lookup"><span data-stu-id="b643d-132">In Listing 1, a single action filter – the `OutputCache` action filter – is applied to the `Index()` method.</span></span> <span data-ttu-id="b643d-133">Se precisar, você poderá aplicar vários filtros de ação à mesma ação.</span><span class="sxs-lookup"><span data-stu-id="b643d-133">If you need, you can apply multiple action filters to the same action.</span></span> <span data-ttu-id="b643d-134">Por exemplo, talvez você queira aplicar os filtros de ação `OutputCache` e `HandleError` à mesma ação.</span><span class="sxs-lookup"><span data-stu-id="b643d-134">For example, you might want to apply both the `OutputCache` and `HandleError` action filters to the same action.</span></span>

<span data-ttu-id="b643d-135">Na Listagem 1, o filtro de ação `OutputCache` é aplicado à ação `Index()`.</span><span class="sxs-lookup"><span data-stu-id="b643d-135">In Listing 1, the `OutputCache` action filter is applied to the `Index()` action.</span></span> <span data-ttu-id="b643d-136">Você também pode aplicar esse atributo à própria classe `DataController`.</span><span class="sxs-lookup"><span data-stu-id="b643d-136">You also could apply this attribute to the `DataController` class itself.</span></span> <span data-ttu-id="b643d-137">Nesse caso, o resultado retornado por qualquer ação exposta pelo controlador seria armazenado em cache por 10 segundos.</span><span class="sxs-lookup"><span data-stu-id="b643d-137">In that case, the result returned by any action exposed by the controller would be cached for 10 seconds.</span></span>

### <a name="the-different-types-of-filters"></a><span data-ttu-id="b643d-138">Os diferentes tipos de filtros</span><span class="sxs-lookup"><span data-stu-id="b643d-138">The Different Types of Filters</span></span>

<span data-ttu-id="b643d-139">O ASP.NET MVC Framework dá suporte a quatro tipos diferentes de filtros:</span><span class="sxs-lookup"><span data-stu-id="b643d-139">The ASP.NET MVC framework supports four different types of filters:</span></span>

1. <span data-ttu-id="b643d-140">Filtros de autorização – implementa o atributo `IAuthorizationFilter`.</span><span class="sxs-lookup"><span data-stu-id="b643d-140">Authorization filters – Implements the `IAuthorizationFilter` attribute.</span></span>
2. <span data-ttu-id="b643d-141">Filtros de ação – implementa o atributo `IActionFilter`.</span><span class="sxs-lookup"><span data-stu-id="b643d-141">Action filters – Implements the `IActionFilter` attribute.</span></span>
3. <span data-ttu-id="b643d-142">Filtros de resultado – implementa o atributo `IResultFilter`.</span><span class="sxs-lookup"><span data-stu-id="b643d-142">Result filters – Implements the `IResultFilter` attribute.</span></span>
4. <span data-ttu-id="b643d-143">Filtros de exceção – implementa o atributo `IExceptionFilter`.</span><span class="sxs-lookup"><span data-stu-id="b643d-143">Exception filters – Implements the `IExceptionFilter` attribute.</span></span>

<span data-ttu-id="b643d-144">Os filtros são executados na ordem listada acima.</span><span class="sxs-lookup"><span data-stu-id="b643d-144">Filters are executed in the order listed above.</span></span> <span data-ttu-id="b643d-145">Por exemplo, os filtros de autorização são sempre executados antes que os filtros de ação e filtros de exceção sempre sejam executados depois de todos os outros tipos de filtro.</span><span class="sxs-lookup"><span data-stu-id="b643d-145">For example, authorization filters are always executed before action filters and exception filters are always executed after every other type of filter.</span></span>

<span data-ttu-id="b643d-146">Os filtros de autorização são usados para implementar a autenticação e autorização para ações do controlador.</span><span class="sxs-lookup"><span data-stu-id="b643d-146">Authorization filters are used to implement authentication and authorization for controller actions.</span></span> <span data-ttu-id="b643d-147">Por exemplo, o filtro autorizar é um exemplo de um filtro de autorização.</span><span class="sxs-lookup"><span data-stu-id="b643d-147">For example, the Authorize filter is an example of an Authorization filter.</span></span>

<span data-ttu-id="b643d-148">Os filtros de ação contêm a lógica que é executada antes e depois que uma ação do controlador é executada.</span><span class="sxs-lookup"><span data-stu-id="b643d-148">Action filters contain logic that is executed before and after a controller action executes.</span></span> <span data-ttu-id="b643d-149">Você pode usar um filtro de ação, por exemplo, para modificar os dados de exibição que uma ação do controlador retorna.</span><span class="sxs-lookup"><span data-stu-id="b643d-149">You can use an action filter, for instance, to modify the view data that a controller action returns.</span></span>

<span data-ttu-id="b643d-150">Os filtros de resultado contêm a lógica que é executada antes e depois que um resultado de exibição é executado.</span><span class="sxs-lookup"><span data-stu-id="b643d-150">Result filters contain logic that is executed before and after a view result is executed.</span></span> <span data-ttu-id="b643d-151">Por exemplo, talvez você queira modificar um resultado de exibição logo antes que a exibição seja processada para o navegador.</span><span class="sxs-lookup"><span data-stu-id="b643d-151">For example, you might want to modify a view result right before the view is rendered to the browser.</span></span>

<span data-ttu-id="b643d-152">Filtros de exceção são o último tipo de filtro a ser executado.</span><span class="sxs-lookup"><span data-stu-id="b643d-152">Exception filters are the last type of filter to run.</span></span> <span data-ttu-id="b643d-153">Você pode usar um filtro de exceção para tratar erros gerados por ações do controlador ou pelos resultados da ação do controlador.</span><span class="sxs-lookup"><span data-stu-id="b643d-153">You can use an exception filter to handle errors raised by either your controller actions or controller action results.</span></span> <span data-ttu-id="b643d-154">Você também pode usar filtros de exceção para registrar erros.</span><span class="sxs-lookup"><span data-stu-id="b643d-154">You also can use exception filters to log errors.</span></span>

<span data-ttu-id="b643d-155">Cada tipo diferente de filtro é executado em uma ordem específica.</span><span class="sxs-lookup"><span data-stu-id="b643d-155">Each different type of filter is executed in a particular order.</span></span> <span data-ttu-id="b643d-156">Se você quiser controlar a ordem na qual os filtros do mesmo tipo são executados, poderá definir a propriedade Order de um filtro.</span><span class="sxs-lookup"><span data-stu-id="b643d-156">If you want to control the order in which filters of the same type are executed then you can set a filter's Order property.</span></span>

<span data-ttu-id="b643d-157">A classe base para todos os filtros de ação é a classe `System.Web.Mvc.FilterAttribute`.</span><span class="sxs-lookup"><span data-stu-id="b643d-157">The base class for all action filters is the `System.Web.Mvc.FilterAttribute` class.</span></span> <span data-ttu-id="b643d-158">Se você quiser implementar um tipo específico de filtro, precisará criar uma classe que herda da classe de filtro base e implementar uma ou mais das interfaces IAuthorizationFilter, IActionFilter, IResultFilter ou ExceptionFilter.</span><span class="sxs-lookup"><span data-stu-id="b643d-158">If you want to implement a particular type of filter, then you need to create a class that inherits from the base Filter class and implements one or more of the IAuthorizationFilter, IActionFilter, IResultFilter, or ExceptionFilter interfaces.</span></span>

### <a name="the-base-actionfilterattribute-class"></a><span data-ttu-id="b643d-159">A classe base ActionFilterAttribute</span><span class="sxs-lookup"><span data-stu-id="b643d-159">The Base ActionFilterAttribute Class</span></span>

<span data-ttu-id="b643d-160">Para facilitar a implementação de um filtro de ação personalizado, a estrutura MVC do ASP.NET inclui uma classe base `ActionFilterAttribute`.</span><span class="sxs-lookup"><span data-stu-id="b643d-160">In order to make it easier for you to implement a custom action filter, the ASP.NET MVC framework includes a base `ActionFilterAttribute` class.</span></span> <span data-ttu-id="b643d-161">Essa classe implementa as interfaces `IActionFilter` e `IResultFilter` e herda da classe `Filter`.</span><span class="sxs-lookup"><span data-stu-id="b643d-161">This class implements both the `IActionFilter` and `IResultFilter` interfaces and inherits from the `Filter` class.</span></span>

<span data-ttu-id="b643d-162">A terminologia aqui não é totalmente consistente.</span><span class="sxs-lookup"><span data-stu-id="b643d-162">The terminology here is not entirely consistent.</span></span> <span data-ttu-id="b643d-163">Tecnicamente, uma classe que herda da classe ActionFilterAttribute é um filtro de ação e um filtro de resultado.</span><span class="sxs-lookup"><span data-stu-id="b643d-163">Technically, a class that inherits from the ActionFilterAttribute class is both an action filter and a result filter.</span></span> <span data-ttu-id="b643d-164">No entanto, no sentido flexível, o filtro de ação do Word é usado para fazer referência a qualquer tipo de filtro na estrutura MVC do ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="b643d-164">However, in the loose sense, the word action filter is used to refer to any type of filter in the ASP.NET MVC framework.</span></span>

<span data-ttu-id="b643d-165">A classe base ActionFilterAttribute tem os seguintes métodos que você pode substituir:</span><span class="sxs-lookup"><span data-stu-id="b643d-165">The base ActionFilterAttribute class has the following methods that you can override:</span></span>

- <span data-ttu-id="b643d-166">OnActionExecuting – esse método é chamado antes da execução de uma ação do controlador.</span><span class="sxs-lookup"><span data-stu-id="b643d-166">OnActionExecuting – This method is called before a controller action is executed.</span></span>
- <span data-ttu-id="b643d-167">OnActionExecuted – esse método é chamado depois que uma ação do controlador é executada.</span><span class="sxs-lookup"><span data-stu-id="b643d-167">OnActionExecuted – This method is called after a controller action is executed.</span></span>
- <span data-ttu-id="b643d-168">OnResultExecuting – esse método é chamado antes da execução de um resultado de ação do controlador.</span><span class="sxs-lookup"><span data-stu-id="b643d-168">OnResultExecuting – This method is called before a controller action result is executed.</span></span>
- <span data-ttu-id="b643d-169">OnResultExecuted – esse método é chamado depois que um resultado de ação do controlador é executado.</span><span class="sxs-lookup"><span data-stu-id="b643d-169">OnResultExecuted – This method is called after a controller action result is executed.</span></span>

<span data-ttu-id="b643d-170">Na próxima seção, veremos como você pode implementar cada um desses métodos diferentes.</span><span class="sxs-lookup"><span data-stu-id="b643d-170">In the next section, we'll see how you can implement each of these different methods.</span></span>

### <a name="creating-a-log-action-filter"></a><span data-ttu-id="b643d-171">Criando um filtro de ação de log</span><span class="sxs-lookup"><span data-stu-id="b643d-171">Creating a Log Action Filter</span></span>

<span data-ttu-id="b643d-172">Para ilustrar como você pode criar um filtro de ação personalizado, criaremos um filtro de ação personalizado que registra em log os estágios de processamento de uma ação do controlador para a janela de saída do Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="b643d-172">In order to illustrate how you can build a custom action filter, we'll create a custom action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span> <span data-ttu-id="b643d-173">Nossa `LogActionFilter` está contida na Listagem 2.</span><span class="sxs-lookup"><span data-stu-id="b643d-173">Our `LogActionFilter` is contained in Listing 2.</span></span>

<span data-ttu-id="b643d-174">**Listagem 2 – `ActionFilters\LogActionFilter.vb`**</span><span class="sxs-lookup"><span data-stu-id="b643d-174">**Listing 2 – `ActionFilters\LogActionFilter.vb`**</span></span>

[!code-vb[Main](understanding-action-filters-vb/samples/sample2.vb)]

<span data-ttu-id="b643d-175">Na Listagem 2, os métodos `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`e `OnResultExecuted()` chamam o método `Log()`.</span><span class="sxs-lookup"><span data-stu-id="b643d-175">In Listing 2, the `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`, and `OnResultExecuted()` methods all call the `Log()` method.</span></span> <span data-ttu-id="b643d-176">O nome do método e os dados de rota atuais são passados para o método de `Log()`.</span><span class="sxs-lookup"><span data-stu-id="b643d-176">The name of the method and the current route data is passed to the `Log()` method.</span></span> <span data-ttu-id="b643d-177">O método `Log()` grava uma mensagem na janela de saída do Visual Studio (consulte a Figura 2).</span><span class="sxs-lookup"><span data-stu-id="b643d-177">The `Log()` method writes a message to the Visual Studio Output window (see Figure 2).</span></span>

<span data-ttu-id="b643d-178">[![gravar na janela de saída do Visual Studio](understanding-action-filters-vb/_static/image5.png)](understanding-action-filters-vb/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="b643d-178">[![Writing to the Visual Studio Output window](understanding-action-filters-vb/_static/image5.png)](understanding-action-filters-vb/_static/image4.png)</span></span>

<span data-ttu-id="b643d-179">**Figura 02**: gravando na janela de saída do Visual Studio ([clique para exibir a imagem em tamanho normal](understanding-action-filters-vb/_static/image6.png))</span><span class="sxs-lookup"><span data-stu-id="b643d-179">**Figure 02**: Writing to the Visual Studio Output window ([Click to view full-size image](understanding-action-filters-vb/_static/image6.png))</span></span>

<span data-ttu-id="b643d-180">O controlador Home na Listagem 3 ilustra como você pode aplicar o filtro de ação de log a uma classe de controlador inteira.</span><span class="sxs-lookup"><span data-stu-id="b643d-180">The Home controller in Listing 3 illustrates how you can apply the Log action filter to an entire controller class.</span></span> <span data-ttu-id="b643d-181">Sempre que qualquer uma das ações expostas pelo controlador Home é invocada – o método `Index()` ou o método `About()` – os estágios de processamento da ação são registrados na janela de saída do Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="b643d-181">Whenever any of the actions exposed by the Home controller are invoked – either the `Index()` method or the `About()` method – the stages of processing the action are logged to the Visual Studio Output window.</span></span>

<span data-ttu-id="b643d-182">**Listagem 3 – `Controllers\HomeController.vb`**</span><span class="sxs-lookup"><span data-stu-id="b643d-182">**Listing 3 – `Controllers\HomeController.vb`**</span></span>

[!code-vb[Main](understanding-action-filters-vb/samples/sample3.vb)]

### <a name="summary"></a><span data-ttu-id="b643d-183">Resumo</span><span class="sxs-lookup"><span data-stu-id="b643d-183">Summary</span></span>

<span data-ttu-id="b643d-184">Neste tutorial, você foi apresentado aos filtros de ação do ASP.NET MVC.</span><span class="sxs-lookup"><span data-stu-id="b643d-184">In this tutorial, you were introduced to ASP.NET MVC action filters.</span></span> <span data-ttu-id="b643d-185">Você aprendeu sobre os quatro tipos diferentes de filtros: filtros de autorização, filtros de ação, filtros de resultado e filtros de exceção.</span><span class="sxs-lookup"><span data-stu-id="b643d-185">You learned about the four different types of filters: authorization filters, action filters, result filters, and exception filters.</span></span> <span data-ttu-id="b643d-186">Você também aprendeu sobre a classe de `ActionFilterAttribute` base.</span><span class="sxs-lookup"><span data-stu-id="b643d-186">You also learned about the base `ActionFilterAttribute` class.</span></span>

<span data-ttu-id="b643d-187">Por fim, você aprendeu a implementar um filtro de ação simples.</span><span class="sxs-lookup"><span data-stu-id="b643d-187">Finally, you learned how to implement a simple action filter.</span></span> <span data-ttu-id="b643d-188">Criamos um filtro de ação de log que registra os estágios de processamento de uma ação de controlador na janela de saída do Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="b643d-188">We created a Log action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="b643d-189">[Anterior](asp-net-mvc-routing-overview-vb.md)
> [Próximo](improving-performance-with-output-caching-vb.md)</span><span class="sxs-lookup"><span data-stu-id="b643d-189">[Previous](asp-net-mvc-routing-overview-vb.md)
[Next](improving-performance-with-output-caching-vb.md)</span></span>
